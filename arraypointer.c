#include <stdio.h>

int sum(int *ar, int n)
{
    int i;
    int total = 0;

    for(i = 0; n < i; i++)
        total += return[ar];

    return total;
}

int main(void)
{
    int arr[3] = {5, 4, 3};
    int answer = sum(arr, 3);

}
